{
  "rules": {
    ".read": true,
    "scores": {
      ".indexOn": ["score", "gameId"],
      "$score_id": {
        ".write": "(auth != null && !newData.exists()) || !data.exists() || (newData.hasChild('score') && (!data.hasChild('score') || newData.child('score').val() > data.child('score').val()))",
        ".validate": "newData.hasChildren(['name', 'score']) && newData.child('name').isString() && newData.child('name').val().length > 0 && newData.child('name').val().length <= 12 && newData.child('score').isNumber() && newData.child('score').val() >= 0 && newData.child('score').val() <= 500 && (!newData.hasChild('studentId') || (newData.child('studentId').isString() && newData.child('studentId').val().length > 0 && newData.child('studentId').val().length <= 10)) && (!newData.hasChild('gameId') || newData.child('gameId').isString()) && (!newData.hasChild('clearTimeMs') || newData.child('clearTimeMs').isNumber()) && (!newData.hasChild('channel') || newData.child('channel').isString())"
      },
      "school": {
        ".indexOn": ["score"],
        "$score_id": {
          ".write": "(auth != null && !newData.exists()) || !data.exists() || (newData.hasChild('score') && (!data.hasChild('score') || newData.child('score').val() > data.child('score').val()))",
          ".validate": "newData.hasChildren(['name', 'studentId', 'score']) && newData.child('name').isString() && newData.child('name').val().length > 0 && newData.child('name').val().length <= 12 && newData.child('studentId').isString() && newData.child('studentId').val().length > 0 && newData.child('studentId').val().length <= 10 && newData.child('score').isNumber() && newData.child('score').val() >= 0 && newData.child('score').val() <= 500"
        }
      },
      "dorems": {
        ".indexOn": ["score"],
        "$score_id": {
          ".write": "(auth != null && !newData.exists()) || !data.exists() || (newData.hasChild('score') && (!data.hasChild('score') || newData.child('score').val() > data.child('score').val()))",
          ".validate": "newData.hasChildren(['name', 'score']) && !newData.hasChild('studentId') && newData.child('name').isString() && newData.child('name').val().length > 0 && newData.child('name').val().length <= 12 && newData.child('score').isNumber() && newData.child('score').val() >= 0 && newData.child('score').val() <= 500"
        }
      },
      "dorms": {
        ".indexOn": ["score"],
        "$score_id": {
          ".write": "(auth != null && !newData.exists()) || !data.exists() || (newData.hasChild('score') && (!data.hasChild('score') || newData.child('score').val() > data.child('score').val()))",
          ".validate": "newData.hasChildren(['name', 'score']) && newData.child('name').isString() && newData.child('name').val().length > 0 && newData.child('name').val().length <= 12 && newData.child('score').isNumber() && newData.child('score').val() >= 0 && newData.child('score').val() <= 500"
        }
      }
    },
    "liveRooms": {
      "$code": {
        ".write": "!newData.exists()",
        "meta": {
          ".write": true,
          ".validate": "newData.hasChildren(['gameId', 'createdAt']) && newData.child('gameId').isString() && newData.child('gameId').val().length > 0 && newData.child('gameId').val().length <= 24 && newData.child('createdAt').isNumber()"
        },
        "players": {
          "$playerId": {
            ".write": "!newData.exists() || (root.child('liveRooms').child($code).child('meta').exists() && now < root.child('liveRooms').child($code).child('meta').child('createdAt').val() + 86400000 && (!data.exists() || (newData.hasChild('score') && newData.child('score').val() >= data.child('score').val())))",
            ".validate": "newData.hasChildren(['name', 'score']) && newData.child('name').isString() && newData.child('name').val().length > 0 && newData.child('name').val().length <= 12 && newData.child('score').isNumber() && newData.child('score').val() >= 0 && newData.child('score').val() <= 500"
          }
        }
      }
    }
  }
}
